/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // EWO-001: sin dominios de imagenes remotas ni configuracion adicional —
  // se agrega cuando un modulo funcional real lo requiera (docs/19, seccion 1).
};

export default nextConfig;
