# EWO-SEC-NAV-001 — T04: Auditoría final independiente

## Control del documento

| Campo             | Valor                                                                                                                                                                                        |
| ----------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Work Order        | `EWO-SEC-NAV-001 — Tenant Isolation & Navigation Contracts`                                                                                                                                  |
| Tarea             | `T04 — Identidad canónica de navegación de CFDI`                                                                                                                                             |
| Decisión          | `D-012 — Identidad canónica de navegación de CFDI mediante documentId`                                                                                                                       |
| Fecha             | 2026-08-06                                                                                                                                                                                   |
| Auditor           | Claude Code (`CLAUDE-CODE-01`)                                                                                                                                                               |
| Tipo              | Auditoría `READ ONLY` — **no es la certificación independiente de Codex que exige `.claude/rules/00-governance.md` regla 5** (ver nota de gobierno abajo)                                    |
| Snapshot auditado | `feature/frontend-ux-audit` · `HEAD b5b289d32fdcc8d7ab61fd62ecfe0316b8c75be8` (árbol de trabajo con cambios locales sin commitear — no penalizado por sí solo; ver §"Aislamiento del delta") |
| Veredicto         | **REQUIERE CAMBIOS** (dos hallazgos `MEDIO`, ninguno sobre la identidad canónica en sí)                                                                                                      |

### Nota de gobierno — quién puede certificar

`.claude/rules/00-governance.md` regla 5 fija que Claude Code puede implementar dentro del alcance autorizado, **pero no puede marcar una tarea como `PASSED`**; esa certificación requiere auditoría independiente `READ ONLY` de **Codex**. `T01` y `T02` fueron certificadas así, con "Auditor: Codex" en su control de documento. Esta misión (`CONTAIA-EWO-SEC-NAV-001-T04-FINAL-AUDIT`) me asigna explícitamente a mí (Claude Code) a producir esta auditoría, por lo que la registro con mi identidad real — **no me atribuyo el rol de Codex** ni reclamo que este documento por sí solo satisface la regla 5. Este artefacto es la evidencia canónica que la misión pidió para decidir posteriormente el estado; la certificación `PASSED` de `T04`/`D-012` sigue pendiente de una auditoría independiente de Codex, exactamente como ocurrió con `T01`/`T02`.

## Confirmación de misión (precondiciones verificadas antes de trabajar)

1. Repositorio: `C:\Users\EliteBook\Desktop\contai\ContaIA` — coincide con `EXPECTED_REPOSITORY`. ✅
2. Rama: `feature/frontend-ux-audit` — coincide con `EXPECTED_BRANCH`. `HEAD` = `b5b289d32fdcc8d7ab61fd62ecfe0316b8c75be8`. ✅
3. `MISSION_ID` activo: `CONTAIA-EWO-SEC-NAV-001-T04-FINAL-AUDIT` — distinto de la misión anterior (`CONTAIA-EWO-SEC-NAV-001-T03-FINAL`, cerrada con entrega propia y sin relación de continuidad con esta). No se retoma trabajo de `T03`. ✅
4. `docs/engineering/audits/EWO-SEC-NAV-001-T04_FINAL_AUDIT.md` no existía antes de esta misión (verificado por `ls` antes de escribir). ✅
5. Ningún archivo existente fue modificado durante esta misión — solo lectura y comandos de validación no destructivos (`grep`, `git diff --check`, `tsc --noEmit`), y la creación de este único artefacto. ✅

## Alcance

Auditoría independiente `READ ONLY` de:

- `T04 — Identidad canónica de navegación de CFDI` de `EWO-SEC-NAV-001`.
- `D-012 — Identidad canónica de navegación de CFDI mediante documentId`.

Verificación del contrato vinculante de `D-012` (9 puntos), consistencia entre `docs/08`, `docs/09`, `docs/14`, `docs/15`, `docs/16`, `docs/17`, `docs/31`, `docs/32`, `EWO-005_DOCUMENTS_FISCAL_PLAN.md`, coherencia con el modelo Prisma real, y revisión del frontend real (`apps/web/src/app/[companyId]/documentos/**`) y del frontend de prototipo (`apps/web/src/app/demo/**`) en cuanto pudieran contradecir la identidad canónica.

## Fuera de alcance

- `T03` y `D-011` — no auditados en esta misión (mission explícitamente lo prohíbe). Su estado documental (`IMPLEMENTADA · PENDIENTE DE AUDITORÍA`, cerrada documentalmente el 2026-08-06 según `EWO-SEC-NAV-001_TENANT_ISOLATION_PLAN.md` §21) se toma como dado, sin reverificar.
- `T05`/`T06` — no iniciadas, no evaluadas.
- Roles y permisos de las pantallas de CFDI/Documentos (`docs/14` §41/§43, `docs/31` `PAGE-0019`/`0021`–`0023`) — pertenecen al dominio de `D-011`/`BR-PERM-004`, no al de `D-012`. Se observaron durante la lectura pero no se evalúan aquí.
- Migraciones, `schema.prisma` (solo se lee para verificar coherencia, no se propone cambio), guards, permisos, `D-009`, `D-010`, `D-011` en sí mismas.
- Cambios locales ajenos presentes en el árbol de trabajo no atribuibles a `T04` (ver aislamiento del delta).

## Aislamiento del delta de T04

El árbol de trabajo tiene 76 rutas con cambios (`git status --porcelain`), ninguna commiteada — patrón consistente con el resto de `EWO-SEC-NAV-001` (`T01`–`T03` se auditaron sobre el mismo tipo de árbol sin commitear). No se penaliza el árbol sucio por sí mismo, conforme a la instrucción de esta misión y al precedente de `EWO-SEC-NAV-001-T01_FINAL_AUDIT.md`/`T02_FINAL_AUDIT.md`.

Atribución del delta, contra el reporte de implementación propio de `T04` (plan §18) y verificación directa de contenido:

| Archivo                                                                                                                                                                                                                     | Atribuible a                         | Evidencia                                                                                                                                                     |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `docs/14_INFORMATION_ARCHITECTURE.md`                                                                                                                                                                                       | `T04`                                | `ROUTE-0019` = ruta canónica; nota de `D-012`/`T04` bajo la tabla de rutas (línea 698)                                                                        |
| `docs/31_MASTER_SCREEN_MAP.md` (fila `PAGE-0020`, columna Ruta)                                                                                                                                                             | `T04`                                | Ruta canónica en `PAGE-0020`                                                                                                                                  |
| `docs/31_MASTER_SCREEN_MAP.md` (filas `PAGE-0019`/`0021`–`0023`, columna Roles)                                                                                                                                             | `T03` (sesión posterior, 2026-08-06) | Ediciones de roles (`Administrador`, `document.read`, etc.) — no tocan identidad ni rutas; fuera de alcance de esta auditoría                                 |
| `docs/32_MASTER_NAVIGATION_ARCHITECTURE.md`                                                                                                                                                                                 | `T04`                                | Árbol de navegación con nodo `/cfdi` bajo `/documentos/{documentId}` y anotación `[D-012: ...]`; `/fiscal/cfdi` anotado explícitamente como listado           |
| `docs/engineering/EWO-005_DOCUMENTS_FISCAL_PLAN.md` (fila de detalle de CFDI)                                                                                                                                               | `T04`                                | Fila `/{companyId}/documentos/{documentId}/cfdi` con referencia a `D-012`                                                                                     |
| `docs/engineering/EWO-005_DOCUMENTS_FISCAL_PLAN.md` (fila de listado de CFDI, línea 80)                                                                                                                                     | **No tocada por T04**                | Sigue en `/{companyId}/fiscal` desde el plan original (2026-07-22); ver Hallazgo T04-H-02                                                                     |
| `brain/DECISIONS.md` (`D-012`)                                                                                                                                                                                              | `T04`                                | Solo `Estado`+`Historial`; Contexto/Problema/Alternativas/Análisis/Decisión/Contrato intactos (verificado por lectura completa de la sección, líneas 556–671) |
| `brain/DECISION_INDEX.md`, `AI_CONTEXT.md`, `PROJECT_INDEX.md`                                                                                                                                                              | `T04`                                | Filas/celdas de `D-012`/`T04` consistentes entre sí                                                                                                           |
| `CHANGELOG.md`                                                                                                                                                                                                              | **`T04` no generó entrada propia**   | Ver Hallazgo T04-H-01                                                                                                                                         |
| `packages/database/prisma/permissions-catalog.ts`, `docs/04`, `docs/08` (`API-0023`–`0028`), `docs/11`, `docs/15` `UXF-0011`, `docs/16` `WF-0012`/`0013`/`0015`/`0016`, `packages/database/src/permissions-catalog.test.ts` | `T03`                                | Contenido de permisos/roles (`document.download`, `cfdi.read`), no de identidad/rutas — fuera de alcance                                                      |
| Resto del árbol sucio (guards, `apps/api/src/common/**`, `apps/web` fuera de documentos, `.claude/**`, archivos de prototipo `MASTER_CONTEXT_v3_*`, `Q001_Decision_Brief.md`, etc.)                                         | Ajeno a `EWO-SEC-NAV-001`            | No relacionado con `documentId`/`cfdiId`/`folioFiscal`/rutas de CFDI; no evaluado                                                                             |

No se detectó código, Prisma ni contrato de API funcional modificado fuera de alcance por `T04` — confirmado por lectura directa: `schema.prisma` sin diferencias relacionadas con `Cfdi`/`Document` más allá de lo ya vigente, y `docs/08_API_DESIGN.md` `API-0027` sin cambio de ruta (solo su columna Permiso fue tocada por `T03`, ajeno a identidad).

## Evidencia inspeccionada

- `brain/DECISIONS.md` — `D-009` (líneas 265–323, estatus `APROBADA · IMPLEMENTADA · PASSED`) y `D-012` completa (líneas 556–671).
- `brain/DECISION_INDEX.md`, `AI_CONTEXT.md`, `PROJECT_INDEX.md`, `CHANGELOG.md`.
- `docs/08_API_DESIGN.md` (`API-0027`, línea 188 y diagrama de secuencia línea 527).
- `docs/09_DATABASE_DESIGN.md` (línea 151, descripción de `Cfdi`).
- `docs/14_INFORMATION_ARCHITECTURE.md` (tabla de rutas completa, líneas 655–698; tabla de trazabilidad, línea 718).
- `docs/15_UX_FLOWS.md`, `docs/16_WIREFRAMES_SPECIFICATION.md`, `docs/17_PROTOTYPE_SPECIFICATION.md` (sin coincidencias de ruta de detalle de CFDI).
- `docs/31_MASTER_SCREEN_MAP.md` (`PAGE-0019`/`PAGE-0020`).
- `docs/32_MASTER_NAVIGATION_ARCHITECTURE.md` (árbol de navegación completo, líneas 60–90).
- `docs/engineering/EWO-005_DOCUMENTS_FISCAL_PLAN.md` (tabla de rutas frontend §2.3, línea 79–81, y encabezado de control).
- `docs/engineering/EWO-SEC-NAV-001_TENANT_ISOLATION_PLAN.md` — completo, con foco en §18 (reporte de `T04`).
- `packages/database/prisma/schema.prisma` — modelo `Cfdi` completo (líneas 620–645): `id`, `companyId`, `documentId`, `folioFiscal`, `issuedAtLocal`.
- `apps/api/src/modules/documents/documents.controller.ts` (rutas reales implementadas: `POST companies/:companyId/documents`, `GET companies/:companyId/documents`, `GET documents/:documentId`, `POST documents/:documentId/confirm-upload` — sin `GET documents/:documentId/cfdi` todavía).
- `apps/api/src/modules/cfdi/` — solo repositorios y persistencia (`cfdi.repository.ts`, `persist-cfdi-aggregate.ts`, etc.), sin controlador.
- `apps/web/src/app/[companyId]/documentos/` completo: `page.tsx`, `documents-view.tsx`, `documents-table.tsx`, `file-dropzone.tsx` y sus pruebas.
- `apps/web/src/lib/documents-client.ts` (llamadas reales a `API-0025`/`API-0026`... solo detalle/confirmación de Documento, sin CFDI).
- `apps/web/src/app/demo/(app)/cfdi/[id]/page.tsx`, `cfdi-detail-view.tsx`, `cfdi-list-view.tsx`, `_lib/mock-data.ts`.

## Búsqueda global

| Patrón buscado                                       | Resultado                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ---------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `/{companyId}/fiscal/cfdi/{cfdiId}`                  | 0 ocurrencias vigentes. 2 ocurrencias, ambas dentro de `EWO-SEC-NAV-001_TENANT_ISOLATION_PLAN.md` §18, tabla "Ruta anterior" — texto histórico explícito, no forma vigente.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `/{companyId}/fiscal/{documentId}`                   | 0 ocurrencias vigentes. 1 ocurrencia, misma tabla histórica de §18.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `/fiscal/cfdi/` (con segmento adicional tras `cfdi`) | 0 ocurrencias — `/fiscal/cfdi` aparece siempre como ruta terminal de listado, nunca seguida de un identificador.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `{cfdiId}` como parámetro de ruta pública            | 0 ocurrencias en `docs/14`, `docs/31`, `docs/32`, `EWO-005_DOCUMENTS_FISCAL_PLAN.md`, `docs/08`. Las 40+ ocurrencias de `cfdiId` restantes en el repositorio son: (a) nombre de campo interno documentado en `docs/09` línea 151 (`Clave primaria: cfdiId (UUID)` — describe `Cfdi.id`, coherente con lo que `D-012` mismo señala en su Contexto: "`Cfdi.id` ... `docs/09` §151 la llama `cfdiId`"); (b) nombre de columna FK interna en `EWO-005_BLOCK_E_ARCHITECTURE_ADDENDUM.md` y `schema.prisma` (`CfdiConcept.cfdiId`, `CfdiTax.cfdiId` — claves foráneas de persistencia, nunca parámetros de URL); (c) texto histórico dentro de `D-012` y del propio plan, citando el problema ya resuelto. |
| `folioFiscal` como parámetro de URL                  | 0 ocurrencias. Todas las apariciones (`docs/08` §13 deduplicación, `docs/09`, `schema.prisma` `@@unique([companyId, folioFiscal])`, `EWO-005_DOCUMENTS_FISCAL_PLAN.md`) son criterio de búsqueda/deduplicación o restricción de base de datos, nunca ruta.                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `ROUTE-0019`                                         | `docs/14` línea 677 (definición) y línea 718 (trazabilidad) — ambas apuntan a la ruta canónica.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `PAGE-0020`                                          | `docs/31` línea 120 (ruta canónica, roles fuera de alcance aquí) y referencias cruzadas en `docs/15`/`docs/16` por ID, sin repetir URL.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `API-0027`                                           | `docs/08` línea 188 (`GET /documents/{documentId}/cfdi`, sin cambio respecto a `D-012` contrato punto 6) y diagrama de secuencia línea 527. Sin controlador implementado todavía (`apps/api/src/modules/cfdi/` no tiene `*.controller.ts`).                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `router.push`/enlaces hacia detalle CFDI             | Ningún enlace en `apps/web/src/app/[companyId]/documentos/**` navega a un detalle de CFDI — la vista real solo lista y sube documentos, sin interacción de fila (`documents-table.tsx` no tiene `<Link>`/`onClick` de navegación). En `apps/web/src/app/demo/(app)/cfdi/`, sí existe navegación real: `cfdi-list-view.tsx:59` construye `href={`/demo/cfdi/${cfdi.id}`}` — ver Hallazgo T04-H-03.                                                                                                                                                                                                                                                                                                    |
| Nombres de rutas antiguas equivalentes               | Ninguna ruta activa adicional encontrada fuera de las ya evaluadas.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |

## Consistencia documental

`docs/14` (`ROUTE-0018`/`ROUTE-0019`), `docs/31` (`PAGE-0019`/`PAGE-0020`, columna Ruta) y `docs/32` (árbol de navegación) convergen exactamente en:

- Listado: `/{companyId}/fiscal/cfdi` (`ROUTE-0018` → `PAGE-0019`), anotado en `docs/32` línea 78 como `[listado de CFDI procesados; el detalle vive en /documentos/{documentId}/cfdi]` — cumple explícitamente la instrucción de esta auditoría de confirmar que `/fiscal/cfdi` se usa solo como listado, nunca como detalle por `cfdiId`.
- Detalle: `/{companyId}/documentos/{documentId}/cfdi` (`ROUTE-0019` → `PAGE-0020`), forma canónica única.

`docs/08_API_DESIGN.md` `API-0027` (`GET /documents/{documentId}/cfdi`) coincide con la forma canónica y no requirió cambio, exactamente como documenta `D-012` contrato punto 6.

**`EWO-005_DOCUMENTS_FISCAL_PLAN.md` no converge en su fila de listado.** Su fila de detalle sí fue corregida por `T04` a la forma canónica (línea 81), pero su fila de listado (línea 80) sigue en `/{companyId}/fiscal`, sin el segmento `/cfdi` que usan `docs/14`/`docs/31`/`docs/32`. Ver Hallazgo T04-H-02.

## Coherencia con API y datos

Verificado por lectura directa de `packages/database/prisma/schema.prisma` (líneas 620–645):

- `Document.id` existe desde la creación del registro (fuera del modelo `Cfdi`, verificado por la propia FK `Cfdi.documentId`).
- `Cfdi.id` es la PK real (campo `id`, no `cfdiId` — `docs/09` usa `cfdiId` como nombre descriptivo, discrepancia ya reconocida por el propio Contexto de `D-012`, no es un hallazgo nuevo).
- `Cfdi.documentId` (`@map("document_id")`) es la FK 1:1 hacia `Document`.
- `Cfdi.folioFiscal` (`@map("folio_fiscal") @db.VarChar(36)`) no tiene exposición en ninguna ruta.
- La relación 1:1 vigente no requiere resolución pública por `cfdiId`: `API-0027` recibe `documentId` en su contrato (`docs/08` línea 188), sin controlador implementado todavía — por lo tanto no hay código que pueda contradecir el contrato en ejecución. La ruta es, por construcción, resoluble sin fila `Cfdi` porque `documentId` es una propiedad del `Document`, existente desde `PENDING_UPLOAD`.
- Sin cambios de `schema.prisma` ni migraciones atribuibles a `T04` (confirmado — `T04` es "solo documentación de arquitectura de información" según su propio reporte, y la inspección de `schema.prisma` no muestra diferencias relacionadas con esta tarea).

## Revisión del frontend real

`apps/web/src/app/[companyId]/documentos/` contiene únicamente `page.tsx` (lista + carga) y sus componentes de apoyo (`documents-table.tsx`, `file-dropzone.tsx`). No existe todavía ninguna subruta `[documentId]/cfdi`, ningún `router.push`/`<Link>` hacia un detalle de CFDI, y `documents-table.tsx` no tiene interacción de fila (ni `onClick` ni `href`). Es decir: **el frontend real todavía no implementa ninguna forma de navegación al detalle de CFDI**, ni la canónica ni una antigua. Conforme a la regla de evaluación de esta misión ("una pantalla futura todavía no implementada no bloquea una tarea documental"), esto **no es un hallazgo** — es ausencia de superficie, no contradicción. El riesgo que `T04` (plan §18) dejó registrado ("frontend de documentos sin versionar puede haberse construido antes de fijar esta ruta canónica") queda **descartado** por esta inspección directa: el código real no contiene ninguna ruta incompatible que corregir.

`apps/web/src/app/demo/(app)/cfdi/[id]/` es una superficie distinta: prototipo con datos simulados (`DEMO_CFDIS` en `_lib/mock-data.ts`), sin `{companyId}`, sin autenticación, sin Membership, sin llamada a la API real. Su ruta es `/demo/cfdi/{id}`, resuelta contra `cfdi.id` del mock, no contra el modelo Prisma real. Ver Hallazgo T04-H-03 para su clasificación.

## Validaciones

| Validación                                                                                 | Comando/método                                                                                                                                                                                                                                                                          | Resultado                                                                                                                                                                                                                                                                                                                                                                                                          |
| ------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Búsqueda global de formas de ruta retiradas y de `cfdiId`/`folioFiscal` como parámetro     | `grep` dirigido (ver tabla de Búsqueda global)                                                                                                                                                                                                                                          | Sin ocurrencias vigentes fuera de las ya evaluadas                                                                                                                                                                                                                                                                                                                                                                 |
| `git diff --check` acotado a los archivos atribuibles a `T04`                              | `git diff --check -- docs/14_INFORMATION_ARCHITECTURE.md docs/31_MASTER_SCREEN_MAP.md docs/32_MASTER_NAVIGATION_ARCHITECTURE.md docs/engineering/EWO-005_DOCUMENTS_FISCAL_PLAN.md docs/engineering/EWO-SEC-NAV-001_TENANT_ISOLATION_PLAN.md brain/DECISIONS.md brain/DECISION_INDEX.md` | Sin advertencias de contenido — solo avisos de fin de línea LF→CRLF, preexistentes en todo el repositorio (mismo patrón en archivos no tocados por ninguna tarea de esta EWO)                                                                                                                                                                                                                                      |
| Coherencia de rutas/enlaces relativos dentro del nuevo artefacto                           | Inspección manual de cada enlace `[...](../...)`                                                                                                                                                                                                                                        | Todos resuelven a archivos existentes verificados en esta misión                                                                                                                                                                                                                                                                                                                                                   |
| Estado documental cruzado (`AI_CONTEXT.md`, `PROJECT_INDEX.md`, `brain/DECISION_INDEX.md`) | Lectura comparada                                                                                                                                                                                                                                                                       | Consistentes entre sí: los tres marcan `T04`/`D-012` como `IMPLEMENTADA · PENDIENTE DE AUDITORÍA`                                                                                                                                                                                                                                                                                                                  |
| `pnpm --filter @contaia/web run typecheck`                                                 | Ejecutado                                                                                                                                                                                                                                                                               | 7 errores, **idénticos** a los ya documentados como ajenos por `EWO-SEC-NAV-001-T02_FINAL_AUDIT.md` (`SessionState` no exportado, `DocumentDto` no exportado, tipado de mock de `fetch`, `afterEach` no declarado) — ninguno referencia `documentId`, `cfdiId`, `folioFiscal` ni rutas de CFDI. No atribuible a `T04` (que no modificó código); no se corrigió por estar fuera del `ALLOWED_WRITE` de esta misión. |
| Infraestructura de validación dirigida de rutas (build/e2e)                                | No ejecutada                                                                                                                                                                                                                                                                            | No aplica: `T04` no tiene código ni ruta implementada que ejecutar (`GET /documents/:documentId/cfdi` sin controlador; frontend sin subruta de CFDI)                                                                                                                                                                                                                                                               |

No se modificó ningún archivo para forzar el paso de una validación.

## Hallazgos

| ID       | Severidad | Ubicación                                                    | Problema                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Evidencia                                                                                                                                                                                                                    | Impacto                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Corrección mínima                                                                                                                                                                                                                                                                                                                                                                                                | Estado                                            |
| -------- | --------- | ------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------- |
| T04-H-01 | **MEDIO** | `CHANGELOG.md`                                               | La implementación de `T04` (2026-08-04) no tiene entrada propia en el historial cronológico. `CHANGELOG.md` registra "Decisiones D-010, D-011 y D-012" (apertura), "Implementación de T01", "Corrección de hallazgos de T01", "Implementación de T02", "Cierre administrativo de T01 y T02" e "Implementación parcial de T03" — todas del mismo 2026-08-04 o posteriores — pero salta directo de "Cierre administrativo de T01 y T02" a "Implementación parcial de T03" sin ningún encabezado para `T04`, pese a que el propio plan (§18) documenta su implementación completa ese mismo día. `CHANGELOG.md` declara en su propio "Propósito" ser el "Historial cronológico y **detallado** de **todos** los cambios técnicos... y cierres de tarea del proyecto". | `grep -n "^## 2026-08-04" CHANGELOG.md` devuelve 6 encabezados, ninguno menciona `T04`; comparado contra `EWO-SEC-NAV-001_TENANT_ISOLATION_PLAN.md` §18, que sí documenta la implementación completa de `T04` el 2026-08-04. | Un lector que recorra `CHANGELOG.md` cronológicamente (uso previsto y documentado del archivo) no se entera de que `T04` ocurrió; podría concluir que sigue pendiente, pese a que `AI_CONTEXT.md`, `PROJECT_INDEX.md` y `brain/DECISION_INDEX.md` sí la reflejan correctamente. No afecta la corrección de la identidad canónica en sí — es una laguna de trazabilidad, no de arquitectura.                                                                                                                                                                                                                                                                                    | Añadir una entrada `## 2026-08-04 — Implementación de T04 (EWO-SEC-NAV-001): identidad canónica de CFDI por documentId`, con el mismo formato que las entradas hermanas de T01/T02/T03, resumiendo la corrección de `ROUTE-0019`/`PAGE-0020`/árbol de navegación/`EWO-005_DOCUMENTS_FISCAL_PLAN.md` y remitiendo a `EWO-SEC-NAV-001_TENANT_ISOLATION_PLAN.md` §18. Cambio de una sola sección, sin tocar código. | ABIERTO                                           |
| T04-H-02 | **MEDIO** | `docs/engineering/EWO-005_DOCUMENTS_FISCAL_PLAN.md` línea 80 | La fila de **listado** de CFDI en la tabla de rutas frontend (§2.3) sigue en `/{companyId}/fiscal`, mientras que `docs/14` `ROUTE-0018`, `docs/31` `PAGE-0019` y el árbol de navegación de `docs/32` usan de forma convergente `/{companyId}/fiscal/cfdi`. `T04` corrigió la fila de **detalle** (línea 81) en el mismo documento pero no tocó esta fila hermana, que data del plan original (2026-07-22, previo a `D-012`).                                                                                                                                                                                                                                                                                                                                       | `grep -n "\{companyId\}/fiscal[^/]"` sobre `docs/` solo devuelve esta línea; `docs/14` línea 676, `docs/31` línea 119 y `docs/32` línea 78 usan `/{companyId}/fiscal/cfdi`.                                                  | Es exactamente la clase de defecto que `D-012` fue creada para eliminar (múltiples formas de ruta incompatibles para la misma pantalla), ahora en la página hermana de listado en vez de la de detalle. La tarea explícita de esta auditoría (Tarea 3) exige confirmar convergencia entre `docs/14`, `31`, `32`, `EWO-005` y `docs/08` — no converge. No reintroduce `cfdiId` ni `folioFiscal` en URL; no contradice el contrato vinculante de `D-012` (que solo gobierna la ruta de detalle). Sin frontend real implementado todavía que haya consumido la forma incorrecta (ver Revisión del frontend real), por lo que el riesgo es documental, no de código en producción. | Corregir la celda de `docs/engineering/EWO-005_DOCUMENTS_FISCAL_PLAN.md` línea 80 a `/{companyId}/fiscal/cfdi`, alineada con `ROUTE-0018`. Cambio de una celda de tabla.                                                                                                                                                                                                                                         | ABIERTO                                           |
| T04-H-03 | **BAJO**  | `apps/web/src/app/demo/(app)/cfdi/[id]/`                     | La ruta de prototipo `/demo/cfdi/{id}` navega por `cfdi.id` (`cfdi-list-view.tsx:59`, `href={`/demo/cfdi/${cfdi.id}`}`) — el mismo patrón de identidad que `D-012` prohíbe para la aplicación real (`{cfdiId}` como parámetro público). No está documentada en `docs/14`/`docs/31`/`docs/32` (no forma parte del corpus que `D-012` gobierna), vive en un namespace separado sin `{companyId}`, sin autenticación ni Membership, y resuelve contra datos simulados (`DEMO_CFDIS`) sin relación con el modelo Prisma real — no es "la aplicación real" que Tarea 5 pide inspeccionar, y su propio título de página se autodeclara "Prototipo ContaIA".                                                                                                              | `apps/web/src/app/demo/(app)/cfdi/[id]/page.tsx` (parámetro `id`, resuelto contra `DEMO_CFDIS.find(item => item.id === id)`); `apps/web/src/app/demo/(app)/cfdi/cfdi-list-view.tsx:59`.                                      | Bajo por ahora: superficie de prototipo aislada, sin usuarios reales, sin datos reales, sin relación con `D-012`. Riesgo latente si el patrón `[id]` se reutiliza al construir la ruta real de CFDI bajo `[companyId]`, en cuyo caso sí violaría el contrato vinculante.                                                                                                                                                                                                                                                                                                                                                                                                       | Ninguna corrección requerida en el prototipo en sí. Si/cuando se implemente la ruta real de detalle de CFDI en `apps/web/src/app/[companyId]/documentos/[documentId]/cfdi/`, verificar explícitamente que no se copie el patrón `[id]`/`cfdi.id` del prototipo.                                                                                                                                                  | ABIERTO, no bloqueante — recomendación preventiva |

Ningún hallazgo de severidad `CRÍTICO` ni `ALTO`. Ninguno de los tres hallazgos contradice el contrato vinculante de `D-012`, reintroduce `cfdiId` como parámetro público en la aplicación real, ni expone `folioFiscal` en una URL — los criterios explícitos de `FAILED` de esta misión no se cumplen.

## Riesgos residuales

- **`RW-06` del plan (reintroducción de `cfdiId` en URLs) — parcialmente vigente, fuera del corpus documental gobernado por `D-012`.** El prototipo `/demo/cfdi/[id]` es el primer código real que usa ese patrón, aunque en una superficie aislada sin relación con la aplicación de producción. `T05` debería decidir explícitamente si el prototipo se retira, se documenta como fuera de alcance de `D-012`, o se migra a la forma canónica antes de que sirva de plantilla para la implementación real.
- **Ninguna prueba automatizada** valida que `docs/14`/`docs/31`/`docs/32`/`EWO-005_DOCUMENTS_FISCAL_PLAN.md` no vuelvan a divergir en sus rutas — mismo riesgo residual que ya registró el propio §18 del plan. `T03` sí introdujo una prueba de sincronización equivalente para permisos (`packages/database/src/permissions-catalog.test.ts`, ver `EWO-SEC-NAV-001_TENANT_ISOLATION_PLAN.md` §21); no existe un control análogo para rutas/identidad, dado que no hay código de rutas todavía que una prueba pueda ejercitar.
- El typecheck global de `apps/web` sigue en rojo por 7 errores ajenos a esta EWO (ya documentados por `T02_FINAL_AUDIT.md`); no bloquea `T04` pero sigue sin resolverse.

## Veredicto final

**REQUIERE CAMBIOS**

La identidad canónica de navegación del CFDI está correctamente implementada y es internamente coherente en todo el corpus documental vigente (`docs/14`, `docs/31`, `docs/32`, `docs/08`) y en el modelo de datos real (`schema.prisma`): `documentId` es la única identidad pública, `cfdiId` no aparece como parámetro de ruta en ninguna fuente canónica, `folioFiscal` nunca aparece en una URL, y el frontend real todavía no implementa ninguna ruta que pudiera contradecirlo. Los tres hallazgos son de **completitud documental** (una entrada faltante en `CHANGELOG.md`, una celda de tabla no actualizada en un plan secundario, y una observación preventiva sobre un prototipo aislado) — ninguno reabre la decisión arquitectónica ni exige revertir o rediseñar nada de lo implementado por `T04`. Los dos hallazgos `MEDIO` son, cada uno, una corrección de una sola línea/celda, sin riesgo técnico.

## Estado recomendado de T04

`T04: REQUIERE CAMBIOS MENORES (documentales) · PENDIENTE DE AUDITORÍA INDEPENDIENTE DE CODEX` — no se recomienda `PASSED` mientras T04-H-01 y T04-H-02 sigan abiertos, dado el criterio mecánico de esta auditoría (todo hallazgo `MEDIO` abierto impide `PASSED`/`PASSED CON OBSERVACIONES`). Ambas correcciones son triviales y no arquitectónicas; una vez aplicadas, T04 debería calificar para `PASSED` en una re-auditoría, sujeta en todo caso a la certificación independiente de Codex que exige la regla de gobierno 5.

## Estado recomendado de D-012

`D-012: IMPLEMENTADA · PENDIENTE DE AUDITORÍA` — sin cambio respecto al estado actual. El contrato vinculante de `D-012` (9 puntos) está satisfecho en su totalidad por la evidencia inspeccionada; los hallazgos de esta auditoría no son defectos del contrato ni de su implementación, sino de completitud del registro documental circundante. No se recomienda ni `PASSED` ni ningún cambio al contrato vinculante.

## Historial

| Fecha      | Evento                                                                                                                                                                                                                                 |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-08-06 | Creación de esta auditoría `READ ONLY` por Claude Code (misión `CONTAIA-EWO-SEC-NAV-001-T04-FINAL-AUDIT`); no modifica código, pruebas, decisiones, Work Order ni estados administrativos. Único archivo creado: este mismo documento. |
